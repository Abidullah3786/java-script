// Rules for naming constants
  // 1) name it in uper case if the value is known before the execution
  const PI = 3.14;
  const NEUTRON_MASS = 1.674927 * 10;

  // 2) name it in camel case if its value is unknown at execution time
  // const webPageLoadTime = someFun(arg)
  
